apiVersion: v1
kind: Service
metadata:
  name: {{ include "645_extracredit.fullname" . }}
  labels:
    {{- include "645_extracredit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "645_extracredit.selectorLabels" . | nindent 4 }}
