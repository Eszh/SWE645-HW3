apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "645_extracredit.fullname" . }}-test-connection"
  labels:
    {{- include "645_extracredit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "645_extracredit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
